{
  "name": "okss",
  "version": "1.0.0",
  "private": true,
  "description": "옥야앱 (OKSS) — 창녕옥야고 학생회 앱. 빌드 스텝 없는 정적 SPA(단일 진입 index.html + css/ + js/). Supabase 백엔드.",
  "scripts": {
    "dev": "python -m http.server 5173",
    "start": "python -m http.server 5173",
    "serve": "npx serve .",
    "copy": "node scripts/copy-web.cjs",
    "sync": "node scripts/copy-web.cjs && cap sync",
    "cap:android": "cap open android",
    "cap:ios": "cap open ios",
    "run:android": "cap run android"
  },
  "keywords": [
    "okss",
    "okya",
    "school",
    "spa",
    "supabase"
  ],
  "license": "UNLICENSED",
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/app": "^8.1.1",
    "@capacitor/browser": "^8.0.4",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
